#include<stdio.h>
/**
 * main - to print numbers
 * return o (sucessful)
*/
int main(void)
{
	int numbers;
	for (numbers = 0; numbers < 10; numbers++)
		putchar((num % 10) + '0');
	print('\n');
return (0);
}

